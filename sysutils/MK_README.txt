# Mk/ symlink placeholder
# When building outside the opnsense/plugins tree, copy or symlink the
# Mk/ directory from https://github.com/opnsense/plugins here.
#
# Quick bootstrap on an OPNsense/FreeBSD build host:
#   git clone https://github.com/opnsense/plugins /tmp/opnsense-plugins
#   ln -s /tmp/opnsense-plugins/Mk ./Mk

